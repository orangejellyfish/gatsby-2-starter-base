import React from 'react';
import { Container } from 'reactstrap';

const NotFoundPage = () => (
  <Container>
    <h1>Not found</h1>
    <p>The route you have tried to access does not exist yet.</p>
  </Container>
);

export default NotFoundPage;
