// This Babel config is only used by Jest when running unit tests. At build time
// Gatsby generates its own Babel config and ignores this file.
{
  "presets": [
    "react",
    "es2015",
    "stage-0"
  ]
}
